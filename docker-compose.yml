version: "3.9"
services:
  api:
    build: api
    ports:
      - "8000:8000"
  ui:
    build: ui
    ports:
      - "5000:80"
  db:
    build:
      context: db
      args:
        - password
    environment:
      - POSTGRES_PASSWORD=${password}
    ports:
      - 5432:5432
